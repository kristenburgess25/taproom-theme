<section id="faqs" data-section-id="{{ section.id }}" data-section-type="faqs" >
    <div class="container">
        <div class="section-header text-center">
            <h2>{{section.settings.title}}</h2>
        </div>
      {% for block in section.blocks %}
        {% if block.settings.question != blank %}
          <div> {{ block.settings.question }}</div>
        {% endif %}
        {% if block.settings.answer != blank %}
          <div>{{ block.settings.answer | escape }}</div>
        {% endif %}
      {% endfor %}
      
        <button class="accordion">Is this a question?</button>
        <div class="panel">
            <p>It most certainly is!</p>
        </div>
        <button class="accordion">Is this also a question?</button>
        <div class="panel">
            <p>Seems likely...</p>
        </div>
    </div>
</section>

<script type="text/javascript">

const acc = document.getElementsByClassName("accordion");
let i;
for (i = 0; i < acc.length; i++) {
    acc[i].addEventListener("click", function() {
        this.classList.toggle("active");
        const panel = this.nextElementSibling;
        
        if (panel.style.maxHeight) {
            panel.style.maxHeight = null;
        } else {
            panel.style.maxHeight = panel.scrollHeight + "px";
        }
    });
}
</script>

{% schema %}
{
  "name": {
    "en": "FAQ"
  },
  "class": "index-section",
  "max_blocks": 9,
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": {
        "en": "Heading"
      },
      "default": {
        "en": "FAQ"
      }
    }
  ],
  "blocks": [
    {
      "type": "faq",
      "name": {
        "en": "FAQ"
      },
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": {
            "en": "Question"
          },
          "default": {
            "en": "Frequent question"
          }
        },
        {
          "type": "text",
          "id": "answer",
          "label": {
            "en": "Answer"
          },
          "default": {
            "en": "Frequent answer"
          }
        }
      ]
    }
  ],
  "presets": [
    {
      "name": {
        "en": "FAQ"
      },
      "category": {
        "en": "Custom Section"
      },
      "blocks": [
        {
          "type": "faq"
        },
        {
          "type": "faq"
        }
      ]
    }
  ]
}
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
