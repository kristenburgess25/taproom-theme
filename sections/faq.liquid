<script src="https://cdn.jsdelivr.net/npm/vue@2.6.10/dist/vue.js"></script>

<section class="faq">
<div class="container">
<div class="faq-outter">
<h2 class="section-heading">Frequently Asked Questions</h2>
</div>
<div id="vue-faq">
    <ul id="faq-container">
        <li v-for="item in items" :key="item.message">
            <faq-item v-bind:item="item"></faq-item>
        </li>
    </ul>
    
</div>
</div>
</section>

<script>
    const FAQ =  {
        props: ['item'],
        delimiters: ['${', '}'],
        template: '<div> <p class="accordion" :class="{active:selected}" @click="handleClick()"> ${ item.question } </p> <p class="panel" v-show="selected"> ${ item.answer } </p> </div>',
        data: function() {
            return {
                selected: false,
            }
        },
        methods: {
            handleClick: function() {
                let selectedStatus = this.selected;
                this.selected = !selectedStatus;
            }
        }
    }

    const app = new Vue({
        el: '#vue-faq',
        data: {
            items: [
                { question: 'Is this a question?', answer: "It most certainly is" },
                { question: 'Is this also a question?', answer: "Bih, it might be!" }
            ]
        },
        delimiters: ['${', '}'],
        components: {'faq-item': FAQ}
    })
</script>

<style scoped>
.accordion {
  background-color:#eee;
  color: #444;
  cursor: pointer;
  padding: 18px;
  width: 100%;
  text-align: left;
  border: none;
  outline: none;
  transition: 0.4s;
}

.active, .accordion:hover {
  background-color: #ccc;
}

.panel {
  padding: 0 18px;
  font-style: italic;
  background-color: white;
  overflow: hidden;
  transition: max-height 0.2s ease-out;
}

.accordion:after {
  content: '\02795'; /* Unicode character for "plus" sign (+) */
  font-size: 13px;
  color: #777;
  float: right;
  margin-left: 5px;
}

.active:after {
  content: "\2796"; /* Unicode character for "minus" sign (-) */
}

h2 {
    text-align: center;
}
</style>


{% schema %}
{
"name": "FAQ",
"settings": [],
"presets": [
{
"name": "FAQ",
"category": "ADVANCED LAYOUT"
}
]
}
{% endschema %}
{% stylesheet %}
{% endstylesheet %}
{% javascript %}
{% endjavascript %}